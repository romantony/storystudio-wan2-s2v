"""
Bazel build rules for Wan2.2 S2V Docker image
Uses shell scripts to wrap Docker build and push commands
"""

# Build and push wrapper
sh_binary(
    name = "build_and_push",
    srcs = ["build_and_push.sh"],
)
